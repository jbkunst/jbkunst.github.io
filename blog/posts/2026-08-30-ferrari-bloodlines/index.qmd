---
title: Ferrari bloodlines
description: An experimental view of Ferrari models, production periods and proposed relationships.
date: 2026-08-30
date-modified: 2026-08-30
categories: [motor-sport, highcharter, data-visualization]
image: images/ferrari-250-gt-swb-preview.jpg
resources:
  - images/ferrari-250-gt-swb-preview.jpg
---

<script>document.body.style.setProperty("--blog-background", "#c41230")</script>

```{r}
#| label: setup
#| include: false

source(here::here("blog", "_R", "post_setup.R"))

install_missing_packages(c(
  "tidyverse",
  "highcharter",
  "gh",
  "httr2"
))
```

This post is part of [`motorsport-legends`](https://github.com/jbkunst/motorsport-legends). It uses Ferrari models to explore families, production periods and relationships across time.

Through this project, I try to organize and store data about vehicles, race results and other motorsport topics that interest me.

## About the data

The main source is Ferrari's [Past Models catalogue](https://www.ferrari.com/en-EN/auto/past-model). The process used to collect the model pages, images, descriptions and available specifications is documented in the [`01-ferrari-past-models.R`](https://github.com/jbkunst/motorsport-legends/blob/main/R/cars/01-ferrari-past-models.R) script.

Part of the resulting dataset is enriched through AI-assisted research, deterministic rules and manual review. This is used particularly to estimate each model's production end year and to propose families and relationships. These values remain under review, and the process is documented in [`99-ferrari-past-model-enrichment.R`](https://github.com/jbkunst/motorsport-legends/blob/main/R/cars/99-ferrari-past-model-enrichment.R).

Model names, images, descriptions, specifications and other source material belong to their respective owners. This post only organizes and displays the information as part of the project.

```{r}
#| label: load-ferrari-data
#| message: false

library(tidyverse)
library(highcharter)

ferrari_data_url <- paste0(
  "https://api.github.com/repos/jbkunst/motorsport-legends/contents/",
  "data/cars/ferrari_past_models_enriched.csv"
)

ferrari_response <- httr2::request(ferrari_data_url) |>
  httr2::req_headers(
    Accept = "application/vnd.github.raw+json",
    Authorization = paste("Bearer", gh::gh_token()),
    `X-GitHub-Api-Version` = "2022-11-28"
  ) |>
  httr2::req_perform()

ferrari <- ferrari_response |>
  httr2::resp_body_string() |>
  I() |>
  readr::read_csv(show_col_types = FALSE)

ferrari
```

The current snapshot contains `r nrow(ferrari)` models grouped into `r n_distinct(ferrari$main_family)` proposed families.

```{r}
#| label: summarise-data-coverage

coverage <- ferrari |>
  summarise(
    models = n(),
    production_period = sum(!is.na(end_year)),
    start_year_only = sum(is.na(end_year)),
    lineage = sum(!is.na(lineage)),
    predecessor = sum(!is.na(predecessor))
  ) |>
  pivot_longer(everything(), names_to = "field", values_to = "models") |>
  mutate(
    field = recode(
      field,
      production_period = "Known production period",
      start_year_only = "Start year only",
      lineage = "Assigned lineage",
      predecessor = "Known predecessor",
      .default = "All models"
    )
  )

coverage
```

Bars represent the production periods currently recorded in the project. Models without a reviewed end year remain points.

## Families before chronology

```{r}
#| label: count-models-by-family

family_summary <- ferrari |>
  count(main_family, sort = TRUE) |>
  mutate(main_family = fct_reorder(main_family, n))

ggplot(family_summary, aes(n, main_family)) +
  geom_segment(
    aes(x = 0, xend = n, yend = main_family),
    linewidth = 1.4,
    colour = "#d9d2c3"
  ) +
  geom_point(size = 3.2, colour = "#c41230") +
  labs(
    title = "Eleven ways through Ferrari history",
    subtitle = "The current taxonomy is useful, but some families still contain very different stories.",
    x = "Models",
    y = NULL
  )
```

These family groupings are exploratory and may change as the project reviews more models and relationships.

## A first timeline

The timeline is an experimental view of the chronology and family structure available in the current dataset.

```{r}
#| label: prepare-ferrari-timeline
#| include: false

family_levels <- ferrari |>
  count(main_family, sort = TRUE) |>
  pull(main_family) |>
  rev()

timeline <- ferrari |>
  mutate(
    family_id = match(main_family, family_levels) - 1L,
    start_date = as.Date(sprintf("%s-01-01", start_year)),
    end_date = as.Date(if_else(
      is.na(end_year),
      NA_character_,
      sprintf("%s-12-31", end_year)
    )),
    relationship = coalesce(predecessor_relationship, "origin or unclassified"),
    lineage_label = coalesce(lineage, "Lineage not assigned")
  )

period_points <- timeline |>
  filter(!is.na(end_year)) |>
  transmute(
    x = datetime_to_timestamp(start_date),
    x2 = datetime_to_timestamp(end_date),
    y = family_id,
    name = model,
    color = "#c41230",
    custom = pmap(
      list(
        family = main_family,
        lineage = lineage_label,
        start = start_year,
        end = end_year,
        engine = engine_architecture,
        layout = engine_location,
        relationship = relationship,
        predecessor = coalesce(predecessor, "—"),
        image = coalesce(image_url, ""),
        description = coalesce(description, "")
      ),
      list
    )
  ) |>
  list_parse()

date_points <- timeline |>
  filter(is.na(end_year)) |>
  transmute(
    x = datetime_to_timestamp(start_date),
    y = family_id,
    name = model,
    custom = pmap(
      list(
        family = main_family,
        lineage = lineage_label,
        start = start_year,
        engine = engine_architecture,
        layout = engine_location,
        relationship = relationship,
        predecessor = coalesce(predecessor, "—"),
        image = coalesce(image_url, ""),
        description = coalesce(description, "")
      ),
      list
    )
  ) |>
  list_parse()
```

```{r}
#| code-fold: true
#| label: show-ferrari-timeline
#| column: screen-inset

tooltip_html <- JS(
  "function () {",
  "  const c = this.point.custom || {};",
  "  const end = c.end ? '–' + c.end : ' · end year pending';",
  "  const image = c.image ? '<img src=\"' + c.image + '\" alt=\"\" style=\"display:block;width:260px;height:146px;object-fit:cover;margin:-12px -14px 12px\">' : '';",
  "  return '<div style=\"padding:12px 14px;min-width:220px\">' +",
  "    image +",
  "    '<div style=\"font-size:16px;font-weight:600;color:#28120f\">' + this.point.name + '</div>' +",
  "    '<div style=\"margin:2px 0 10px;color:#8f1d23\">' + c.start + end + '</div>' +",
  "    '<div><b>Family</b> · ' + c.family + '</div>' +",
  "    '<div><b>Lineage</b> · ' + c.lineage + '</div>' +",
  "    '<div><b>Engine</b> · ' + (c.engine || 'pending') + ' · ' + (c.layout || 'pending') + '</div>' +",
  "    '<div><b>Previous</b> · ' + c.predecessor + '</div>' +",
  "    '<div><b>Relation</b> · ' + c.relationship.replaceAll('_', ' ') + '</div>' +",
  "  '</div>';",
  "}"
)

highchart() |>
  highcharter::hc_size(height = 900) |>
  hc_add_dependency("modules/xrange.js") |>
  hc_chart(
    backgroundColor = "#f5f1e8",
    zoomType = "x",
    spacing = c(28, 24, 22, 12),
    style = list(fontFamily = "IBM Plex Sans, sans-serif")
  ) |>
  hc_title(
    text = "Ferrari bloodlines",
    align = "left",
    style = list(color = "#28120f", fontSize = "28px", fontWeight = "300")
  ) |>
  hc_subtitle(
    text = "Production periods are bars. Unfinished research remains visible as points.",
    align = "left",
    style = list(color = "#765f56", fontSize = "14px")
  ) |>
  hc_xAxis(
    type = "datetime",
    min = datetime_to_timestamp(as.Date("1947-01-01")),
    gridLineWidth = 1,
    gridLineColor = "#ded7ca",
    lineColor = "#9b8e80",
    tickColor = "#9b8e80"
  ) |>
  hc_yAxis(
    categories = family_levels,
    title = list(text = ""),
    gridLineColor = "#ded7ca",
    labels = list(style = list(color = "#4a3731", fontSize = "12px"))
  ) |>
  hc_plotOptions(
    series = list(
      states = list(
        inactive = list(opacity = 1)
      )
    )
  ) |>
  hc_add_series(
    type = "xrange",
    name = "Known production period",
    data = period_points,
    borderColor = "#f5f1e8",
    borderWidth = 1.2,
    borderRadius = 1,
    pointWidth = 10,
    states = list(
      hover = list(
        brightness = 0.08,
        borderColor = "#ffffff",
        borderWidth = 2
      )
    )
  ) |>
  hc_add_series(
    type = "scatter",
    name = "End year pending",
    data = date_points,
    color = "#c41230",
    marker = list(
      radius = 4,
      symbol = "circle",
      lineWidth = 1.5,
      lineColor = "#c41230",
      fillColor = "#f5f1e8"
    )
  ) |>
  hc_tooltip(
    useHTML = TRUE,
    outside = TRUE,
    shared = FALSE,
    followPointer = TRUE,
    backgroundColor = "#fffdf8",
    borderColor = "#c9bdae",
    borderRadius = 8,
    shadow = TRUE,
    style = list(zIndex = 9999),
    formatter = tooltip_html
  ) |>
  hc_legend(
    align = "center",
    verticalAlign = "bottom",
    itemStyle = list(color = "#4a3731", fontWeight = "400")
  ) |>
  hc_credits(
    enabled = TRUE,
    text = "Data: motorsport-legends · work in progress",
    href = "https://github.com/jbkunst/motorsport-legends"
  )
```

---

This is an experimental representation of the data currently available in `motorsport-legends`, including estimated production periods and provisional relationships.

## References

- [Ferrari Past Models](https://www.ferrari.com/en-EN/auto/past-model)
- [Bloodlines: Flat-12 Ferraris](https://www.ferrari.com/es-ES/magazine/articles/bloodlines-flat-12-ferraris)
- [Scuderia Ferrari Formula 1 history](https://www.ferrari.com/en-EN/formula1/history)
- [Ferrari 250 GT Berlinetta passo corto](https://www.ferrari.com/en-EN/auto/250-gt-berlinetta-passo-corto) — preview image, credit: Ferrari S.p.A.
